"""pharmpipe — automated pharmacophore-model construction pipeline.

Known-actives assembly (scrape -> site-align -> effect grouping -> efficacy
resolution) is complete; see CLAUDE.md. The next stage, pharmacophore
construction from the grouped active poses (features, clustering,
pharmacophore), is scaffolded and now being implemented.
"""

__version__ = "0.1.0"
